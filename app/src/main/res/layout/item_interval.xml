<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:background="@drawable/rounded_background"
    android:layout_marginBottom="8dp">

    <!-- Иконка для перетаскивания -->
    <ImageView
        android:id="@+id/dragHandle"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_drag_handle"
        android:contentDescription="Перетащить интервал"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="8dp" />

    <!-- Выпадающий список для типа интервала -->
    <Spinner
        android:id="@+id/intervalTypeSpinner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:padding="8dp"
        android:background="@drawable/rounded_background" />

    <!-- Поле ввода расстояния -->
    <EditText
        android:id="@+id/distanceEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:hint="Расстояние (м)"
        android:inputType="numberDecimal"
        android:textSize="14sp"
        android:padding="8dp"
        android:layout_marginStart="8dp" />

    <!-- Кнопка удаления -->
    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_delete"
        android:contentDescription="Удалить интервал"
        android:background="?attr/selectableItemBackground"
        android:layout_marginStart="8dp" />

</LinearLayout>